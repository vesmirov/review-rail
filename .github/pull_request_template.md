## What

## Why

## Checklist

- [ ] `npm test` passes
- [ ] bug fixes include a regression test
- [ ] UI changes: mockup approved, screenshot attached
- [ ] `src/lib/` stays pure (no `chrome.*`), all requests go through `api()`
